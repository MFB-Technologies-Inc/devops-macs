# Secrets — never commit
*.env
secrets.env
.envrc

# Local agent install artifacts (the agent lives in $HOME, but guard against
# accidental in-repo extraction)
azagent/
*.tar.gz

# macOS noise
.DS_Store
